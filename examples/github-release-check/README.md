# GitHub Release Check

两个可独立打包的发布样例：`v1` 和 `v2`。ID 均为 `dev.example.github-release-check`，仅申请 `ui.dom`。启用后右上角显示版本标记，停用或卸载后移除；不读文件、不联网、不启动 Host。

这是用于导入、更新和版本比较的本地测试素材。打包格式见[插件规范](../../docs/spec/plugin-format.md)，导入与更新行为见[管理契约](../../docs/spec/management.md)。不要把项目源代码归档当作插件构建资产。
