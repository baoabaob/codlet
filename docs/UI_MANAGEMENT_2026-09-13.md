# 插件管理界面与更新（2026-09-13）

前一轮调整了插件列表、导入、详情、确认页和验收面板，并交付语言支持和 Codlet 自身更新机制。后续审查发现样式继承、控件状态和信息层级仍存在不一致，当前的组件核对及整改规范见 [官方前端组件核对与 Codlet 界面规范](OFFICIAL_UI_STYLE_2026-09-13.md)。下文保留前一轮实现及验证记录，不代表后续 UI 重构已经完成。

2026-09-14 的原生布局统一、顶部导航、筛选、持久设置与版本页合并见 [本轮实现和验证](LAYOUT_AND_SETTINGS_2026-09-14.md)。

## 安装须知与 GUI 卸载说明（2026-09-18）

- 手动导入本地或 GitHub 插件、手动确认更新时，在实际提交前显示独立“安装须知”对话框；原有来源信任和权限选择仍须完成。取消、关闭及 Esc 均不提交安装，来源或预览变化会使旧确认失效。
- “让 Codex 检查”创建带 Codlet 技能的可编辑任务草稿，附上当前预览目录以及选定的仓库、版本、资源和校验值。检查要求聚焦潜在危险或恶意行为，提供代码位置、证据和触发条件，不运行或安装待检查插件。
- GUI 自身及 Core 返回的依赖链中会影响 GUI 的插件，在详情页卸载位置显示“无法在GUI插件中卸载自己（及其依赖）／如需卸载可以使用CLI或使用Codex”。普通插件仍可从详情页移除。
- “使用Codex”创建带该插件名称、ID 和 Codlet 技能的卸载草稿，先核对来源及依赖影响，再向用户确认；内置插件若不能单独卸载，应说明并提供停用方式。以上入口只创建草稿，不自动发送任务或执行卸载。
- 本地构建、类型检查及 52 项相关自动化测试通过；使用实际构建产物的浏览器预览核对了浅色／深色对话框和卸载说明。本节不代表已完成当前桌面客户端的实机验收。

## 界面

- 标题旁显示版本；主列表使用搜索栏、单一导入入口和刷新图标，移除重复的 Codlets 标题。
- 搜索支持 ID、名称、描述和多个关键词，兼容中英文元数据。悬停只显示描述。
- 导入页使用同一返回按钮和本地/GitHub 选项。路径输入停顿 400ms 后自动检查，旧结果不会覆盖新输入；切换语言保留表单和授权选择。
- 社区链接只放在导入页，以带下划线及外链图标的链接显示。
- 详情显示名称、描述、ID、版本、来源和实际已授予的权限；空权限范围不列 None。源目录通过文件夹图标在 Explorer 中打开，不常驻展示路径。
- 移除确认默认保留源文件及插件数据，可单独勾选删除源目录。登记或目录身份变化时不会误删替换目录；位置消失时正常注销并提示跳过删除。系统、共享注册表及运行时目录受到保护。
- 官方插件名称、描述和 GUI 文案跟随客户端实际语言：中文使用中文，其他语言先使用英文。测试插件不做翻译。

## 文件夹选择修复

在本机对同一个真实目录执行无窗口 COM 测试，原始 `\\?\` 路径使 `SHCreateItemFromParsingName` 返回 `0x80070057`。转换为普通盘符路径后，该调用及 `SetFolder` 均成功。选择器现在保留系统选项、使用专用 STA，并将默认目录作为可回退的提示。

实际界面复测还发现：Windows 文件选择器会初始化 Desktop 命名空间，隔离用户目录缺少 `home/Desktop` 时仍会弹出“位置不可用”。测试环境初始化现会创建这个空目录；旧隔离环境只有通过独占租约及上一实例退出记录验证后才补齐目录，同名文件和链接会被拒绝。修复后已在测试客户端实际打开选择器，选取仓库内测试插件并自动识别成功。

## 更新

已先核对当前官方客户端实际代码，见 [官方流程核对](OFFICIAL_UPDATE_FLOW_2026-09-13.md)。官方 Windows 的检查通常包括下载和暂存；本轮按需求保留 Codlet 的显式“下载 → 安装并重启”交互。安装确认说明会中断当前客户端的本地任务。

Codlet 目前没有已发布更新源，默认不向虚构地址检查、不伪造最新版。发布步骤和更新清单格式见 [运行时更新](RUNTIME_UPDATES.md)。版本区仅显示 Codlet、当前客户端和 Codlet 适配版本；[客户端更新全部交由官方机制](OFFICIAL_CLIENT_UPDATES.md)。

更新包仅包含相应客户端可执行文件、Node 和其校验信息；不会替换插件目录或用户数据。安装前校验当前文件、暂存文件、启动器和进程身份，使用安装目录级互斥和明确交接；失败时根据可确认的状态恢复或保留证据。

## 验证

- Rust 完整回归：539 项通过；原有真实客户端验收门禁保持忽略。
- 后续隔离目录修复：31 项相关回归通过，包含新增的旧环境迁移及文件/链接拒绝用例；Clippy 和可执行文件构建再次通过。
- 固定 Node 24.21.0 完整回归：189 项通过，含更新 helper 与真实 PowerShell 启动器夹具。
- 本地浏览器检查了列表、导入、详情、移除确认、中文/英文回退、深色模式及 480px 窗口；修复了样式覆盖 `hidden` 导致按钮仍可见的问题。
- 真实系统 COM 测试复现并修复文件夹选择的参数错误；实际测试客户端已验证选择文件夹后自动识别插件、中文名称、详情权限、移除确认的默认保留选项，以及资源管理器打开对应源目录。没有执行插件导入或移除。

更新源尚未发布，因此线上更新下载没有可供验收的真实发布版本；包格式、校验、安装交接、恢复及重启通过本地包和独立夹具验证。
