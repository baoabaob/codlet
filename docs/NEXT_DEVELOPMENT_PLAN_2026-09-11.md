# 下一阶段计划：插件 UI、Desktop Adapter、导入与跨平台

状态：2026-09-20 更新。M2–M4、M5a 管理、M5b GitHub 安装和一键更新已实现；安全模式、诊断、运行时 skill 和可读的单版本安装目录已加入开发试用。当前继续 Windows Dev 试用修正，补充通用流量接管基础与 Adapter 接入；安装交付、签名和发布延后。

## 2026-09-20 通用能力补充

按[流量接管实施计划](TRAFFIC_INTERCEPTION_PLAN_2026-09-20.md)推进 Core 的显式 HTTP(S)/SSE 与 WS/WSS 通道、真实请求/响应和双向消息处理原语，并由官方 Desktop Adapter 提供客户端接入方式。API 切换策略、自动重试与错误恢复是具体插件的职责，不纳入 Core。能力必须报告真实覆盖范围，页面 CDP、AppServer RPC 和模型 HTTP 请求不可混为一谈。

同步从用户与插件作者角度整理能力缺口，再按 Core / Adapter / 普通插件分层排序；脑暴不自动扩大本次实现范围。Windows 继续执行测试，macOS 实机验收仍待具备环境；Linux 继续暂缓，不制作发行包。

## 2026-09-16 当前执行顺序

1. 更新和存储整理已部署 Windows Dev；继续保留用户启用状态、配置和权限
2. [架构复审](ARCHITECTURE_REVIEW_2026-09-16.md)：修复 skill 生成中断与清理、统一客户端 profile、拆出共用目录所有权接口，运行全量回归
3. [macOS 开发](MACOS_DEVELOPMENT_2026-09-16.md)：原生启动、IPC、Host、CLI、安全模式及前端 profile 已实现；在真实 Mac 补齐功能和视觉验收
4. 用户继续 Windows Dev 试用，按反馈修复；暂不制作发行包或安装器

当前范围是 Windows x64 / ARM64 和 macOS Apple Silicon；Linux 明确暂缓。旧 Windows 构建和历史回滚的既有实验记录保留其时间属性。下面 9 月 11–15 日的阶段方案用于解释演进，平台状态及工作优先级以上述当前顺序为准；GitHub 插件正常更新只保留当前安装，不再提供历史版本列表。

完整平台目标与官方桌面客户端一致：Windows x64 / ARM64、macOS Apple Silicon，以及后续的 Linux x64 / ARM64。系统、发行版、官方依据和当前验收状态统一记录在 [平台支持矩阵](PLATFORM_SUPPORT.md)；Linux 的目标登记不改变此前暂缓实施的安排。

## 已确认的产品方向

Codlet 继续定位为依托官方客户端的轻量插件运行时，沿用现有 capability kernel、registry、权限、RPC、generation 与生命周期 receipt。社区生态的新增工作集中在分发、发现、兼容性说明和维护规则。

Codlet 内置简单的“插件”页面；发现交给 GitHub Topics 与社区目录。当前不建设独立 Codlet Market，也不引入另一套插件管理内核或独立管理器应用。

| 部分 | 职责 |
| --- | --- |
| Codlet | 导入、显示来源/版本/权限/兼容性、启停、重载、移除，以及明确的更新/回退操作 |
| GitHub | 作者维护源码、发布构建好的版本包、保存变更记录、通过统一 Topic 帮助发现 |
| 社区目录 | 插件介绍、分类、推荐、维护状态和导入入口；可以只是 Markdown 或静态网页 |

目录中的推荐与来源信息不代表执行授权。来自任何入口的同一插件都经过相同的本地检查和生命周期流程。

## 建议执行顺序

| 顺序 / 阶段 | 工作包 | 完成标准 |
| --- | --- | --- |
| 1：M3/M4 收尾 | 当前构建的新建/冷恢复兼容问题；补齐文件修改和权限审批的实机往返 | 普通使用流程不依赖验收任务预热；已声明的实机分支有明确结果 |
| 同期：P0 跨平台准备 | 盘点系统绑定，建立 OS / 架构 / 官方客户端构建矩阵，设计最小平台接口 | 明确哪些逻辑可复用、哪些需要原生实现，以及各平台的实机条件 |
| 2：M3.1 / M4.1 | 首批可复用 UI renderer 功能和 Desktop 语义接口扩展 | 管理 GUI 与一个独立示例使用同一公开 API，完成生命周期与实机检查 |
| 3：M5a 插件管理 | “插件”页面、本地文件夹导入、权限查看/撤销、启停/重载/移除 | GUI 与 CLI 操作同一 registry、授权记录和 receipt，失败结果可查询 |
| 4：M5b 分发与社区 | GitHub 发布包导入、来源/版本记录、显式更新/回退、发布规范、Topic 与社区目录入口 | 一个作者可独立发布，用户从链接导入并管理；目录无需专有安装服务 |
| 5：M5c Private Alpha | Codlet 运行时安装/升级/卸载、测试签名、安全模式、诊断包，集中关闭发布门禁 | 对声明支持的 Windows 构建具备完整交付与实机证据 |
| 后续：P1 平台移植，进入 M6 按平台验收 | 在 P0 证据基础上逐个平台打通启动、Host、IPC、打包和 UI/Backend 适配 | 每个 OS / 架构有独立通过记录；未通过的平台不标为支持 |

UI/Adapter 的首批扩展与本地导入页面可以交替推进：管理页提供真实需求，通用 UI helper 先在它和一个普通插件中验证。后续 UI/Adapter 功能继续按版本迭代，不把一套完整 UI 框架作为 M5 的前置条件。

下一轮直接可执行的 todo：

- [x] 修复测试协调器的严格配置与禁用 app-tools 配置缺项，完成当前构建原生新建/冷恢复，保留同一 Desktop 连接；见[兼容补验](DESKTOP_COMPATIBILITY_2026-09-11.md)。
- [ ] 补齐独立文件修改审批的实机批准/拒绝；权限请求的批准/拒绝、服务端确认及重复 token 拒绝已通过。
- [x] 交付 `context.ui` 和普通 UI 示例；现已迁移官方组件，并通过已核验的 `codex.ui.navigation.page@1` 接入侧栏和整页，替代早期标题栏浮窗。
- [x] 交付 `selection.get`、选择事件、`threads.open` 原生冷恢复和拦截器启停/诊断；见 [M3.1/M4.1 开发与验收说明](UI_HELPERS_AND_NAVIGATION_2026-09-11.md)。
- [x] 明确本地开发目录与托管安装目录的来源/所有权语义，移除保留文件与历史。
- [x] 交付 [GitHub 发布包规范、兼容性元数据、导入/更新/回滚流程](GITHUB_PLUGIN_DISTRIBUTION.md)及[社区目录模板](COMMUNITY_PLUGINS.md)。
- [x] 完成 [P0 平台依赖清单、最小接口草案和验收矩阵](PLATFORM_P0_AUDIT_2026-09-11.md)；实际接口抽取及 P1 移植尚未开始。
- [x] 实现 M5a、M5b；原生界面按手测指南验收。
- [ ] M5c 集中实现安装交付并关闭发布门禁；解释与手测条件见[剩余验收指南](REMAINING_ACCEPTANCE_MANUAL_2026-09-12.md)。

### 2026-09-15 当前进度与接续顺序

| 工作包 | 当前进度 | 接下来 |
| --- | --- | --- |
| M5a 管理界面 | 官方 Apps SDK UI、侧栏整页、插件/设置、搜索筛选、i18n、权限/移除、更新提示已交付，用户确认基本通过；空列表留白单独修正 | 按问题回归，结束持续布局改造 |
| M5b GitHub | 导入、来源历史、更新/回退与启动发布检查已实现；真实公开 release 的 Native 导入、启动发现与热更新，以及 Core 历史回滚已通过 | 按手测表继续覆盖未执行边界分支，不把主流程通过扩展成所有用例通过 |
| M5c 诊断 | 新增 [本地诊断包导出](DIAGNOSTIC_BUNDLES.md)，只读、精简字段、不覆盖原文件 | 随便携/测试包提供恢复入口 |
| M5c 恢复 | [手动安全模式](SAFE_MODE.md)、设置中的安装目录/错误日志入口、有限轮转日志已实现；版本区精简为三个本地字段，移除客户端更新检查 | 正式启动场景仍按发布门禁单独验收 |
| M5c 交付 | 已有便携构建与 Codlet 自身更新机制，尚无完整安装/卸载交付 | 用户级安装/升级/卸载 → 签名与发布验收 |
| 发布门禁 | 正式启动、100 次冷启动、官方入口纯净性、Host crash 与独立 fileChange 仍按各自证据保留 | 不在当前日常客户端运行中强制退出或伪造通过 |
| 跨平台 | P0 接口已抽取，P1 Mac 原生实现和前端 profile 已加入 | 保持 Windows 回归，在 Mac 进行原生功能与视觉验收 |
| 插件创建与运行时技能 | “创建插件”接入原生草稿；Core 提供唯一 `/codlet`，GUI 关闭时仍可用 | 验证跨平台冷启动、目录恢复与退出清理 |

M5c 尚未完成，Private Alpha 尚未达到发布标准。当前 UI 使用体验通过，不等同于全部发布门禁通过。

## M3.1：UI renderer 与 UI Adapter

这一轮把已有 GUI 中可复用的行为变成插件作者能使用的接口。

| 层 | 首批扩展 | 验证重点 |
| --- | --- | --- |
| 通用 renderer UI helpers | 按钮、开关、设置行、确认框、加载/错误状态；事件、焦点、快捷键注销与资源清理 | 普通 DOM 事件本地处理；卸载后没有 listener、observer、timer 或样式残留 |
| 可选 UI Adapter | 版本化外观角色、设置 surface，以及经过实机确认的菜单/侧栏/状态区挂载位置 | 私有 selector、主题字段和平台布局差异集中维护；缺失位置能明确报告 |
| 消费者插件 | 内容、配置、业务动作与显示 | 管理 GUI 与一个普通 UI 插件复用同一接口，权限独立于控件外观 |
| 后续展示扩展 | 消息旁操作、折叠、标注等 presentation transform | 显示变化与已保存 Thread/Turn/Item 历史分开说明 |

首批只选管理页和示例实际需要的少量角色/挂载位置；接口名在验证后冻结。若某个宿主位置没有稳定、可诊断的结构，就不提前承诺支持。组件逻辑不依赖宿主 React/Radix 私有注册，跨 world 不传递 DOM/React 对象。

实机检查包括明暗主题、字体/缩放、窄窗口、键盘与焦点、宿主重建、插件重载与多窗口。现有详细设计继续作为依据：[UI Adapter 能力提案](UI_ADAPTER_CAPABILITIES.md)。

## M4.1：Desktop Adapter

| 优先级 | 扩展 | 条件 |
| --- | --- | --- |
| 首批 | 读取当前选中任务/回合及其变化；定位、打开、恢复任务 | 沿用实际 Desktop 连接和 UI 状态，保留 thread/turn/item 标识和 owner/follower 区别 |
| 首批 | 拦截器管理与诊断：来源、顺序、耗时、失败原因、当前是否可用 | 继续在实际提交前运行，卸载/超时后不能恢复迟到提交 |
| 后续 | 任务创建、附件/更丰富输入、模型及技能选择、更多已验证的 server-request | 每项独立核对 schema、授权、事件与 UI 往返后再公开 |
| 持续维护 | 更细的 capability 可用性、构建兼容说明、已知降级路径 | 某项失效只撤下对应能力；保持当前连接，不改用独立后端 |

当前 v1 的回合写入仍要求“已加载 local owner 任务、文本输入”；`threads.open` 提供进入原生页面并等待 Native 恢复的入口，不绕过这些条件。展示变换属于 renderer；修改后端权威历史不随展示扩展自动获得支持。上述业务映射继续留在可选 Adapter，Core 不增加 Codex 私有方法或模型字段。

## M5a / M5b：导入和管理方案

2026-09-12：M5a 已交付；M5b 的 GitHub 下载、托管来源与历史、显式更新/回滚、发布规范和社区入口已实现。用户选择将真实界面验收改为手测，分别见 [M5a 手测](LOCAL_PLUGIN_MANUAL_TEST_2026-09-11.md)与 [M5b 手测](GITHUB_PLUGIN_MANUAL_TEST_2026-09-12.md)。没有提供公开插件 release，本轮使用固定 HTTP/ZIP 和运行时夹具验证，没有上传远程测试仓库。M0/M1 等发布门禁继续保留。

2026-09-15：按用户授权创建临时公开测试仓库并发布两个版本，补齐真实 Native 导入、启动发现和热更新；Core 回滚保留历史。当前测试安装保留 1.0.0 停用实例，便于复验“有插件更新”的界面。

“插件”页面保留三个直接入口：

1. **从本地文件夹导入**：选取已有 `codlet.json` 目录，检查入口、权限与依赖，展示预览后注册。开发目录继续引用原路径，明确是否启用 watch。
2. **从 GitHub 链接导入**：解析仓库或 release 链接，选择具体 release 和插件构建资产，展示来源、版本、平台、兼容性与权限，确认后安装至 Codlet 自己管理的目录。
3. **浏览社区插件**：打开 GitHub Topic 或社区目录网页；返回 Codlet 使用同一导入流程。

已安装列表显示名称、右侧版本与下方描述，支持搜索和启用筛选；运行状态说明已从常态列表移除，ID 与权限放入详情，文件夹用打开按钮提供。提供启停、重载、查看权限、撤销、移除，以及托管安装版本的显式更新/回退。

导入服务只负责来源解析、预览、下载、校验和文件落地。注册及后续激活仍交给现有管理机制，GUI 和 CLI 共用操作结果。更新先准备候选，检查权限变化与依赖，再走已有代次替换和补偿流程；已提交操作通过原 receipt 查询，不自动重放。

本地开发目录的“移除”只取消注册并停用，保留作者文件。托管安装目录与插件数据分别记录所有权；清除下载包、删除插件数据必须有明确操作语义。不能因来源换了仓库或插件 ID 相同就静默继承旧来源的信任。

### 发布包与兼容性

发行资产包含构建好的 JS、`codlet.json`、所需资源、许可证和说明；导入时不执行仓库构建脚本或安装脚本。普通仓库源码链接如果没有可识别发布包，应解释原因，并提供手动下载/本地导入途径。

记录所选仓库、release/tag、具体资产及内容摘要。更新时展示版本差异、权限增减、依赖与兼容性变化；用户主动选择更新。内容摘要用于识别下载内容，不能替代对插件作者的信任。

兼容性元数据需要分别描述：

- 插件包版本与兼容的 Codlet SDK/runtime 版本；
- capability API 版本与依赖；
- OS、CPU 架构及所需系统能力；
- 声明兼容的 Codex 构建、实际验证过的构建和已知限制；
- 作者、源码、许可证、反馈入口及维护状态。

M5b 使用可选的 `codlet-package.json` 声明 runtime API、平台、作者与 opaque adapter 信息；原有 `codlet.json`、capability 依赖模型和非 semver 的插件版本保持兼容。许可证/反馈/维护信息由发布包 README 和社区目录记录；缺少声明显示未知。下载器不引入 npm 式依赖解析或静默安装其他插件；缺失依赖清楚列出。准确字段与边界见[发布格式](GITHUB_PLUGIN_DISTRIBUTION.md)。

GitHub 提供按 release 读取信息和列出资产的 API，可据此解析下载候选；具体版本和资产选择属于 Codlet 的导入规则。[GitHub Releases API](https://docs.github.com/en/rest/releases/releases)

### 社区发现与维护规则

统一发现标签建议使用仓库 Topic `codlet-plugin`；这是待采用的社区约定，未创建或登记专属市场。GitHub 的 Topics 本身用于按主题发现相关仓库；版本发布仍使用 release/tag。[GitHub Topics 文档](https://docs.github.com/en/repositories/managing-your-repositorys-settings-and-features/customizing-your-repository/classifying-your-repository-with-topics)

社区目录的最小条目包括插件名称、简介、分类、作者/仓库、发布入口、权限摘要、支持平台、兼容/已测构建、最近核验日期和维护状态。维护规则包括：

- 作者随版本更新权限、兼容性、变更记录及反馈入口。
- 目录区分“作者声明”“目录实际验证”“未知/失效”，推荐不等于兼容性通过。
- 长期未维护、上游失效或作者移交时更新状态和来源说明，保留可追溯记录。
- 下架/标记问题影响目录推荐，不在用户机器上自动停用或删除已安装插件。
- 社区入口可提供导入链接，也可提供复制即用的 CLI 命令；CLI 和 GUI 使用同一解析与授权流程。

GitHub CLI 现提供 `releases`、`preview`、`install`、`update`、`history`、`rollback` 和 `community`；准确参数见[CLI 说明](GITHUB_PLUGIN_DISTRIBUTION.md)。GUI 接收复制的 release URL。可选的 `codlet://` 深链接尚未实现；即使后续加入也不能跳过授权或自动执行远程脚本。Codlet 不承担社区账户、评分交易或独立 Market 服务。

## P0 / P1：跨操作系统适配

现在就做 P0，防止新增导入路径、配置存储和 SDK 把 Windows 假设扩散到通用逻辑。完整移植按平台单独交付。

当前代码证据：`src/lib.rs` 中 Host runtime、JS runtime、OS broker、管理 CLI 等模块仍受 `cfg(windows)` 限定；`Cargo.toml` 的系统绑定位于 Windows 依赖；运行时 pin 和打包流程仍面向 Windows。存在某个架构的 Node pin 不等于该架构的 Codlet 已通过验收。

| 可保持共享 | 需要逐平台实现/核验 |
| --- | --- |
| manifest、registry 模型、capability/权限契约、generation/receipt 状态机、JSON RPC、JS API | 官方客户端发现、可验证启动路径、参数及环境继承 |
| 通用导入解析、包元数据与内容校验规则 | 数据目录、路径/链接处理、文件锁、原子替换 |
| renderer helpers 与大部分 Adapter 语义代码 | Host 子进程/进程组、退出与资源回收、authenticated 本地 IPC、对端身份 |
| 共同测试场景和诊断 DTO | Node 产物/许可证、签名、安装升级卸载、平台 UI 差异与 Desktop build 探针 |

先定义最小平台接口，用现有 Windows 行为做第一份实现，保持内核与生命周期语义。非 Windows 平台需要各自的 OS 资源控制办法，不把 Windows Job Object 或 named pipe 当成跨平台保证。仅通过编译不足以宣布支持。

P0 为 Windows x64、Windows arm64、macOS 和 Linux 分别登记 OS/架构、官方客户端可用性、启动机制、测试设备和阻塞项。macOS/Linux 的官方客户端可用性须在开展移植时按官方当时信息核对；本文不提前声明其支持状态。P1 的先后顺序按这些证据和测试条件确定。

P1 从一个目标平台做完整纵切：启动 → Host/renderer → 插件导入/启停 → 当前 Desktop 读写 → 正常退出/故障回收，再扩展到下一个平台。每个平台单独记录 M0/M1 与 Adapter 兼容性门禁，Windows 已有结果不平移为其他平台的通过证明。新增平台不会要求重做插件生态协议或另建管理内核。

## 发布与当前交付的区别

M3/M4 候选与首批 M3.1/M4.1 已交付，不代表 M5 或非 Windows 平台已完成。M0/M1 重复运行、官方入口纯净性和 Runtime Host crash 等历史门禁继续保留，在发布阶段集中处理；日常改动按影响范围验证，不为每个文档或界面小改动重跑全套。

计划最初于 2026-09-11 登记；现已推进兼容修复、P0 盘点、UI/任务导航首批扩展，以及 M5a/M5b。下一工作包为 M5c；独立 fileChange 审批仍按实机适用条件单独补验，不把未触发的分支算作通过。
