# 技能问号弹窗与侧栏图标

插件管理页的版本号旁增加官方 `QuestionMarkCircle` 问号，点击打开模态框。内容为“使用 Codlet 技能，在对话中管理插件，或者询问关于 Codlet 的任何问题”，按钮为“试一下！”。按钮沿用已验证的原生技能草稿入口；底部技能说明区移除。

弹窗使用现有 Radix 依赖及插件所属的 Portal，按钮和图标沿用 Apps SDK UI。支持关闭按钮、Escape、背景点击和焦点循环；关闭时返回触发按钮，页面卸载时弹窗随 UI owner 清理。样式使用宿主语义色，窄窗口保留边距。

按最新选择，侧栏恢复此前的官方 Cube 方块图标；标题大图继续使用 B 款原稿。小尺寸品牌图标保留为备选资产，当前导航入口不使用。

验证：32 项相关导航、UI 生命周期和 GUI 回归检查通过，TypeScript 检查与前端/客户端构建通过。浏览器检查了浅色、深色、480 px 窄窗口，以及 Escape 焦点返回、Tab 焦点循环、试一下关闭弹窗、底部内容移除，控制台无错误。

已部署到现有 Windows 隔离测试客户端并实测：问号打开居中模态框，“试一下！”关闭弹窗并进入带原生橙色 Codlet 技能标签的入门草稿，未发送任务。侧栏实际显示专用剪影，标题显示原稿。

部署 run：`1789535425967-60420`；二进制 SHA-256：`94EA5309F8F5937DC892C9A485BFA08D372938F676D394E6812F9E4A8CD88512`。备份为 `.codlet-artifacts/version-source-2026-09-15/help-dialog-small-icon-backup`，部署验证插件配置、偏好和日常客户端进程保持原样。

## 插件更新提示

按最新反馈，有更新时仅显示“全部更新 (N)”按钮，删除额外的可更新文字、蓝色圆点和重复数量提示。保留检查进度、失败或无更新时的反馈。

最新修改通过 40 项现有控制器测试、TypeScript 和构建；浅色/深色页面及图标对比检查确认原稿轮廓一致、眼睛移除、更新区不再出现额外提示。

最新测试部署：run `1789536598698-60236`，SHA-256 `E77636A95DDD15301AA3E7BB3B0040264ADC0D1810F4C43353885EBEAC00E343`；备份 `original-contour-no-eye-backup`。测试客户端正常启动，插件配置和偏好校验通过。

侧栏恢复 Cube 后的部署：run `1789545222389-2876`，SHA-256 `6D469B970ECD26FF64475AC090586FD15572F8CAF2E725625BAA7964F45CF180`，备份 `restore-sidebar-cube-backup`。前端及客户端构建通过，启动与配置校验通过。
